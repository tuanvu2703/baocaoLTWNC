<header class="bg-primary py-4 px-6 flex items-center justify-between">
	<h1 class="text-2xl font-bold">Category Management</h1>
	<button id="addCategoryBtn"
		class="bg-secondary text-secondary-foreground hover:bg-secondary/80 px-4 py-2 rounded-md bg-sky-300">Add
		Category
	</button>
</header>

<div class="bg-background text-primary-foreground flex flex-col">
	<div class="flex-1 p-6">
		<ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
			<% for(let i=0; i < data.length; i++) {%>
				<li class="bg-card rounded-lg overflow-hidden shadow-lg max-w-[350px]">
					<img src="https://placehold.co/300?text=Category+Image" alt="Category Image"
						class="w-full h-40 object-cover">
					<div class="p-4">
						<h2 class="text-lg font-semibold mb-2">
							<%= data[i].category_name %>
						</h2>
						<h3 class="mb-2">"
							<%= data[i].description %>
								"
						</h3>
						<p class="text-sm text-muted-foreground">Total Products: <%= data[i].total_products %>
						</p>
						<div class="mt-4 flex justify-between">
							<button class=" underline text-gray-900">View Products</button>
							<button class=" underline text-blue-600">Edit</button>
							<button class=" underline text-red-600">Delete</button>
						</div>
					</div>
				</li>
				<% } %>
		</ul>
	</div>
</div>
<!-- MODAL ADD -->
<!-- <div id="modal" class="fixed inset-0 items-center justify-center bg-black bg-opacity-50 hidden">
	<div class="bg-card p-6 rounded-lg shadow-lg">
		<h2 class="text-xl font-semibold mb-4">Add New Category</h2>
		<form id="categoryForm">
			<label class="block mb-2">
				<span class="text-muted-foreground">Category Name</span>
				<input type="text" class="mt-1 block w-full border border-border rounded-md p-2" required>
			</label>
			<div class="flex justify-end">
				<button type="button" id="closeModal"
					class="bg-destructive text-destructive-foreground hover:bg-destructive/80 px-4 py-2 rounded-md mr-2">Cancel</button>
				<button type="submit"
					class="bg-secondary text-secondary-foreground hover:bg-secondary/80 px-4 py-2 rounded-md">Add
					Category</button>
			</div>
		</form>
	</div>
</div> -->
<!-- SCRIPT OPEN MODAL -->
<!-- <script>
	const addCategoryBtn = document.getElementById('addCategoryBtn');
	const modal = document.getElementById('modal');
	const closeModal = document.getElementById('closeModal');
	const categoryForm = document.getElementById('categoryForm');
	addCategoryBtn.addEventListener('click', () => {
		modal.classList.remove('hidden');
	});
	closeModal.addEventListener('click', () => {
		modal.classList.add('hidden');
	});
	categoryForm.addEventListener('submit', (e) => {
		e.preventDefault();
		// Handle form submission logic here
		modal.classList.add('hidden');
		alert('Category added!'); // Placeholder for actual functionality
	});
</script> -->